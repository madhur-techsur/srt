## F01: Request Storage

**Description:** The Request Storage feature is the backend persistence layer for the SRT system. The Spring Boot backend exposes a `POST /api/requests` endpoint that accepts a JSON payload, performs server-side validation, assigns a system-generated unique ID and creation timestamp, persists the record to the H2 in-memory database, and returns the stored record as confirmation. Data persists for the duration of the application session only; no cross-restart durability is required.

---

**Terminology:**

- **Request Record:** A single persisted entity containing `id`, `name`, `title`, `description`, and `createdAt` fields.
- **System-assigned ID:** A server-generated unique identifier (auto-incremented Long) assigned at write time; not provided by the client.
- **Creation Timestamp:** UTC timestamp set by the server at the moment of persistence; not provided by the client.
- **Session Persistence:** Data is available for the lifetime of the running JVM process; cleared on application restart.
- **Server-side Validation:** Validation logic within the Spring Boot controller/service layer; acts as the authoritative gate regardless of client behavior.

---

**Sub-features:**

- `POST /api/requests` REST endpoint
- Server-side required-field validation for `name`, `title`, `description`
- Auto-assignment of unique integer `id` on insert
- Auto-assignment of `createdAt` UTC timestamp on insert
- Persistence to H2 in-memory JPA entity store
- HTTP 201 response with full stored record on success
- HTTP 400 response with descriptive error body on validation failure
- HTTP 500 response on unexpected server error

---

**Process:**

1. Spring Boot controller receives `POST /api/requests` with `Content-Type: application/json`.
2. Controller deserializes the request body into a `RequestDto` object (`name`, `title`, `description`).
3. If deserialization fails (malformed JSON) → return HTTP 400 with error code `INVALID_JSON`.
4. Service layer trims whitespace from all three string fields.
5. Service layer validates each trimmed value is non-empty:
   - If `name` is blank → add field error for `name`.
   - If `title` is blank → add field error for `title`.
   - If `description` is blank → add field error for `description`.
   - If any errors exist → return HTTP 400 with error code `VALIDATION_FAILED` and a list of field-level errors.
6. Service layer constructs a `Request` JPA entity:
   - `id`: auto-generated by database sequence (not set by caller).
   - `name`: trimmed value from DTO.
   - `title`: trimmed value from DTO.
   - `description`: trimmed value from DTO.
   - `createdAt`: `Instant.now()` (UTC) at time of service call.
7. Repository saves entity to H2 store.
8. Service returns the saved entity (with assigned `id` and `createdAt`).
9. Controller serializes entity to JSON and returns HTTP 201 Created with the full request record.

---

**Inputs:**

- HTTP `POST /api/requests`
- Request body (JSON):
  - `name` (string, required): Requester's name.
  - `title` (string, required): Short request title.
  - `description` (string, required): Full request description.

**Example Request Body:**
```json
{
  "name": "Alice Johnson",
  "title": "New desk required",
  "description": "I need a standing desk for ergonomic reasons."
}
```

---

**Outputs:**

- **HTTP 201 Created** — stored request record:
```json
{
  "id": 1,
  "name": "Alice Johnson",
  "title": "New desk required",
  "description": "I need a standing desk for ergonomic reasons.",
  "createdAt": "2026-05-08T14:00:00Z"
}
```
- **HTTP 400 Bad Request** — validation failure:
```json
{
  "errorCode": "VALIDATION_FAILED",
  "message": "One or more fields failed validation.",
  "fieldErrors": [
    { "field": "name", "message": "must not be blank" }
  ]
}
```
- **HTTP 400 Bad Request** — malformed JSON:
```json
{
  "errorCode": "INVALID_JSON",
  "message": "Request body could not be parsed."
}
```
- **HTTP 500 Internal Server Error** — unexpected error:
```json
{
  "errorCode": "INTERNAL_ERROR",
  "message": "An unexpected error occurred."
}
```

---

**Validation:**

- `name`: required; must not be blank after trimming. No format restriction.
- `title`: required; must not be blank after trimming. No format restriction.
- `description`: required; must not be blank after trimming. No format restriction.
- All three fields must be present in the JSON payload (absent fields treated as blank).
- Server-side validation is independent of and does not rely on client-side validation.
- `id` and `createdAt` are server-assigned; any client-provided values for these fields are ignored.
- No length limits enforced at server for MVP; database column constraints apply (see `Y0-schema.md`).

---

**Error States:**

| Scenario | HTTP Status | Error Code | Message |
|----------|-------------|------------|---------|
| All fields valid | 201 | — | (success, record returned) |
| One or more fields blank | 400 | `VALIDATION_FAILED` | "One or more fields failed validation." |
| Malformed JSON body | 400 | `INVALID_JSON` | "Request body could not be parsed." |
| Missing `Content-Type` header | 415 | `UNSUPPORTED_MEDIA_TYPE` | "Content-Type must be application/json." |
| Unexpected server exception | 500 | `INTERNAL_ERROR` | "An unexpected error occurred." |

---

**API Surface (this feature):** `POST /api/requests`. See `Y1-api.md` §POST /api/requests for full request/response schema.

**Schema Surface (this feature):** Writes to `requests` table. See `Y0-schema.md` §requests.
